server:
  port: 8080

spring:
  application:
    name: api-gateway
  main:
    web-application-type: reactive

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true
      routes:
        - id: ai-core
          uri: lb://ai-core
          predicates:
            - Path=/api/ai/**

        - id: code-analyzer
          uri: lb://code-analyzer
          predicates:
            - Path=/api/analyzer/**

        - id: bugfinder
          uri: lb://bugfinder
          predicates:
            - Path=/api/bug/**

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true